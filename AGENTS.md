* commit often, with nit/feat/chore prefixes and descriptive messages
* use unit-tests when appropriate. when using e2e tests use http://.../app/index.html#test_mode=true to speed tests up
* see [tests/README.md](tests/README.md) for test inventory, fixtures, and coverage setup
* update user-facing docs/ when adding features
* when asked to plan, generate a file in plan/ folder
* when implementing a complex feature (only), create a report in reports/ folder