* move 'mobile on desktop' localhost test mode to the advanced settings
* 'zoom to all' in the kebab menu does not encompass all tracks, only first one.
* implement keyboard nabigation in the track list
* the track being edited should be easier to find in the panel, 

continue, with tweaks:
"On restore from persistence, orphan tracks are rebuilt via `buildWorkspaceFromTracks()`."
remove all legacy*-related code and other dead code found. if legacy stuff is detected, delete it. do make sure that persisted tracks can be re-read with a unit-test if not yet done.