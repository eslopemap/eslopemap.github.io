how should I use blend: multiply for raster overlays ? should I put it in the catalog for each or just use it everywhere ? is the normal useful for some of my layers ? ask the right questions and outline possible implementation
no code change  yet except this: to specify multiply any blend mode layer with a small “×” in the settings' layers debug panel
also tell me how we could get the correct order for all layers including system layers in the layer order panel, and be able to move everything around.
both into a new ./plan/ymd-...