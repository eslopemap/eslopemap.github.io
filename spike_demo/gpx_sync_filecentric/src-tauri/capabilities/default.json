{
  "identifier": "default",
  "description": "Default permissions for the GPX sync spike demo.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
